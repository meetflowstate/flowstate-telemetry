version: 2

project_name: flowstate-telemetry

before:
  hooks:
    - go mod tidy

builds:
  - id: flowstate-telemetry
    main: .
    binary: flowstate-telemetry
    env:
      - CGO_ENABLED=0
    # NOTE: Windows targets (windows/amd64, windows/arm64) are intentionally
    # absent — they are scheduled for a follow-up branch alongside the
    # Windows trust/service/PAC implementation in internal/platform/windows.go.
    # Adding them here without the platform code would ship a binary whose
    # `proxy install` command returns errNotImplemented, which is worse than
    # not shipping the target. See workstream-A plan §"Goreleaser changes".
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/meetflowstate/flowstate-telemetry/cmd.version={{.Version}}
      - -X github.com/meetflowstate/flowstate-telemetry/cmd.commit={{.Commit}}
      - -X github.com/meetflowstate/flowstate-telemetry/cmd.date={{.Date}}

archives:
  - id: default
    format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

brews:
  - name: flowstate-telemetry
    repository:
      owner: meetflowstate
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: "https://www.flowstate.inc"
    description: "Configure AI coding tools to emit telemetry to Flowstate"
    license: "MIT"
    install: |
      bin.install "flowstate-telemetry"
    test: |
      system "#{bin}/flowstate-telemetry", "status"

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

release:
  github:
    owner: meetflowstate
    name: flowstate-telemetry
  prerelease: auto
